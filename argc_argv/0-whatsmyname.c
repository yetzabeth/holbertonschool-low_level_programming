#include "main.h"
#include <stdio.h>
/**
 * main - main fuction
 * @argc: saves the memory address
 * @argv: pointer to pointer
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
